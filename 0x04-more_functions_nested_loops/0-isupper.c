#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: the chracter being checked.
 *
 * Return: 1 if character is upper
 *	0 otherwie.
*/

int _isupper(int c)
{
	if (c >= 'A';
	c <= 'Z';
	c++)
		return (1);
	else
		return (0);
}
